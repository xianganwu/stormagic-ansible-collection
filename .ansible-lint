# .ansible-lint

# args[module] warns about 'host' param on SvKMS modules because
# ansible-lint cannot resolve module_defaults with action groups.
# skip_list is needed instead of warn_list because CI uses --strict
# which promotes warnings to errors.
skip_list:
  - args[module]

exclude_paths:
  - .github/
  - .ansible/
  - tests/integration/
  - ansible_collections/
  - docs/superpowers/
  - changelogs/changelog.yaml
  - molecule/*/molecule.yml
