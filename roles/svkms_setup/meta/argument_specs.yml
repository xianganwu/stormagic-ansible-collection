---
argument_specs:
  main:
    short_description: Initial setup and configuration for SvKMS
    description:
      - Creates the initial admin user on a StorMagic SvKMS server.
      - Configures default key access policies.
      - Generates a root CA certificate for the SvKMS instance.
    options:
      svkms_setup_admin_username:
        description: Username for the initial admin account.
        type: str
        default: admin
      svkms_setup_admin_password:
        description: Password for the initial admin account.
        type: str
        required: true
        no_log: true
      svkms_setup_admin_email:
        description: Email address for the initial admin account.
        type: str
        default: admin@example.com
      svkms_setup_default_policies:
        description: >-
          List of default key policies to create. Each entry should include name,
          rotation_days, and key_algorithm.
        type: list
        elements: dict
        default:
          - name: default-rotation-policy
            rotation_days: 365
            key_algorithm: AES-256
          - name: high-security-policy
            rotation_days: 90
            key_algorithm: AES-256
      svkms_setup_ca_common_name:
        description: Common name for the root CA certificate.
        type: str
        default: StorMagic SvKMS Root CA
      svkms_setup_ca_organization:
        description: Organization name for the root CA certificate.
        type: str
        default: StorMagic Ltd
      svkms_setup_ca_validity_days:
        description: Validity period in days for the root CA certificate.
        type: int
        default: 3650
